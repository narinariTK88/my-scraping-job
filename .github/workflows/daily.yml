name: Chiebukuro-Scraper-v2

on:
  schedule:
    - cron: '0 10 * * *'  # 日本時間の19:00 (UTC 10:00) -> URL収集
    - cron: '16 15 * * *' # 日本時間の00:16 (UTC 15:16) -> 詳細解析
  workflow_dispatch:      # 手動実行用

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリを読み込む
        uses: actions/checkout@v3

      - name: Pythonのセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 必要なライブラリをインストール
        run: |
          pip install requests beautifulsoup4 pandas lxml

      # 19:00(UTC 10:00)の場合：URLの取得のみ実行
      - name: URL収集フェーズ
        if: github.event.schedule == '0 10 * * *'
        run: python main.py --mode save_urls --pages 2

      # 00:16(UTC 15:16)または手動実行の場合：保存されたURLを解析
      - name: 詳細解析フェーズ
        if: github.event.schedule == '16 15 * * *' || github.event_name == 'workflow_dispatch'
        run: python main.py --mode analyze --margin 1.0 --len 100

      - name: 結果を保存
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add *.csv
          # コミットする変更がある場合のみコミット
          # [skip ci] を含めることで、自身によるコミットでアクションが再起動するのを防ぐ
          git commit -m "Update data [skip ci]" || exit 0
          # リモートの最新の変更を取り込んでからPush
          git pull --rebase origin main
          git push origin main
